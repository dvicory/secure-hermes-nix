{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "summary": {
      "type": "string",
      "description": "Concise description of the outcome and important decisions."
    },
    "tests": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Verification commands run and their outcomes."
    },
    "remaining_issues": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Known failures, risks, or work that remains."
    },
    "artifacts": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^output/[^/].*"
      },
      "description": "Workspace-root paths below output/ selected for human delivery. Only normalized output/... entries are honored."
    }
  },
  "required": ["summary", "tests", "remaining_issues"],
  "additionalProperties": false
}
