From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Daniel Vicory <dvicory@gmail.com>
Date: Wed, 29 Jul 2026 00:00:00 +0000
Subject: [PATCH] feat(hermes): complete immutable task workspace handoffs

Cut Hermes and the Gondolin broker over to fail-closed task-run activation,
immutable completion capture, native selected-attachment delivery, and
broker-owned workspace lifecycle control. Parent task dependencies remain
metadata-only and each child acquires its own task-scoped workspace.
---
diff --git a/agent/prompt_builder.py b/agent/prompt_builder.py
index b6573d1a3dcd884e05b06c125065d7a752fdac32..ee4a5cd4d73c34151ab73144a9fd006f5c5723a2 100644
--- a/agent/prompt_builder.py
+++ b/agent/prompt_builder.py
@@ -257,13 +257,15 @@ KANBAN_ORCHESTRATOR_GUIDANCE = (
     "dispatcher-assigned task worker.\n"
     "1. Use the `kanban_*` tools to create, inspect, link, comment on, and "
     "observe tasks. Do not execute a task body yourself or substitute "
-    "`delegate_task` for board work the user requested.\n"
+    "`delegate_task` for board work the user requested. A request for a "
+    "downloadable artifact or durable task is board work even when the user "
+    "says only “delegate a task.”\n"
     "2. Create each intended task once. A `ready` or `running` task is queued "
     "or executing, not evidence that the dispatcher is unavailable or the "
-    "worker is stalled. Short tasks need not emit heartbeats. Do not create a "
-    "replacement merely because status or heartbeat is unchanged within the "
-    "claim lease. Judge progress from authoritative task events, not a terminal "
-    "process view.\n"
+    "worker is stalled. Completion notifications are push-driven: after "
+    "creating a task, end the turn instead of polling `kanban_show`, checking "
+    "heartbeats, sleeping, or scheduling a speculative follow-up. Do not create "
+    "a replacement merely because status is unchanged within the claim lease.\n"
     "3. Worker lifecycle belongs to the assigned worker. Do not complete, "
     "block, or heartbeat on its behalf. Unblock only when the task contract "
     "or a human authorizes a retry and the recorded blocker is resolved.\n"
@@ -279,7 +281,13 @@ KANBAN_ORCHESTRATOR_GUIDANCE = (
     "6. Use `kanban_comment` for relevant routing context. Never put secrets, "
     "tokens, raw PII, host paths, or broker authority in board data; task history "
     "is durable.\n"
-
+    "7. Broker completion automatically materializes and sends the worker's "
+    "selected artifacts. Never recreate, base64-encode, execute code for, "
+    "`kanban_attach`, or emit `MEDIA:` copies of those files. The completion "
+    "notification and `kanban_attachments` are authoritative.\n"
+    "8. Only when a human explicitly asks to receive attachments from an "
+    "already completed task, call `kanban_attachments` with `action=\"deliver\"`; "
+    "do not recreate their bytes or rerun the worker."
 )
 
 
diff --git a/gateway/kanban_watchers.py b/gateway/kanban_watchers.py
index d6e324e446083a3662f9658a9010e7b7768b0e1e..a2647912fc1ce3e8133f537fc18c4374a0b7665a 100644
--- a/gateway/kanban_watchers.py
+++ b/gateway/kanban_watchers.py
@@ -654,14 +654,11 @@ class GatewayKanbanWatchersMixin:
         board: Optional[str] = None,
         run_id: Optional[int] = None,
     ) -> None:
-        """Upload the explicit, durable completion artifact manifest.
-
-        Native completions already copied selected files into
-        ``task_attachments``. Broker completions retain only a relative
-        ``output/...`` manifest and an opaque capture id; this method prepares,
-        reads, releases, and stores each export exactly once before sending it
-        to the chat. No path discovery from summaries or result prose is
-        permitted.
+        """Upload artifacts already materialized in native task attachments.
+
+        Broker capture completes only after its selected frozen files have
+        entered the same attachment store. Recipient delivery never reads a
+        live worker workspace or calls the broker.
         """
         if not isinstance(event_payload, dict):
             return
@@ -675,94 +672,19 @@ class GatewayKanbanWatchersMixin:
         if not artifact_names or task is None:
             return
 
-        async def _native_paths() -> list[str]:
-            def _read_native() -> list[str]:
-                from hermes_cli import kanban_db as _kb
-                with contextlib.closing(_kb.connect(board=board)) as conn:
-                    attachments = {
-                        str(attachment.filename): str(attachment.stored_path)
-                        for attachment in _kb.list_attachments(conn, task.id)
-                        if attachment.uploaded_by != "workspace_export"
-                    }
-                return [
-                    attachments[name] for name in artifact_names
-                    if name in attachments and os.path.isfile(attachments[name])
-                ]
-            return await asyncio.to_thread(_read_native)
-
-        async def _broker_paths() -> list[str]:
-            def _copy_broker() -> list[str]:
-                from hermes_cli import kanban_db as _kb
-                from tools.environments.gondolin import BrokerClient
-                import hashlib
-
-                with contextlib.closing(_kb.connect(board=board)) as conn:
-                    capture = _kb.get_workspace_capture(
-                        conn, task.id, run_id
-                    )
-                    if not isinstance(capture, dict):
-                        return []
-                    handoff_id = capture.get("handoff_id")
-                    finalization_id = capture.get("finalization_id")
-                    if not isinstance(handoff_id, str) or not isinstance(finalization_id, str):
-                        return []
-                    existing = {
-                        name: _kb.find_workspace_export_attachment(conn, task.id, name)
-                        for name in artifact_names
-                    }
-                    pending = [name for name in artifact_names if existing[name] is None]
-                    if pending:
-                        endpoint = os.environ.get(
-                            "GONDOLIN_EFFECT_CONTROL_SOCKET", ""
-                        ).strip()
-                        if not endpoint:
-                            return [
-                                str(existing[name].stored_path)
-                                for name in artifact_names
-                                if existing[name] is not None
-                            ]
-                        client = BrokerClient(endpoint, timeout=30.0)
-                        try:
-                            for name in pending:
-                                delivery_id = "d_" + hashlib.sha256(
-                                    f"{finalization_id}:{task.id}:{run_id}\x00{name}".encode()
-                                ).hexdigest()[:32]
-                                prepared = client.prepare_export(
-                                    delivery_id=delivery_id,
-                                    handoff_id=handoff_id,
-                                    relative_path=name.removeprefix("output/"),
-                                )
-                                try:
-                                    data = client.read_export_bytes(
-                                        prepared["exportToken"],
-                                        expected_size=int(prepared["size"]),
-                                    )
-                                finally:
-                                    try:
-                                        client.release_export(prepared["exportToken"])
-                                    except Exception as release_exc:
-                                        logger.warning(
-                                            "kanban notifier: export release failed for %s: %s",
-                                            name, release_exc,
-                                        )
-                                existing[name] = _kb.store_workspace_export_attachment(
-                                    conn, task.id, filename=name, data=data,
-                                    delivery_id=delivery_id, run_id=run_id, board=board,
-                                )
-                        finally:
-                            client.close()
-                    return [
-                        str(existing[name].stored_path)
-                        for name in artifact_names
-                        if existing[name] is not None
-                        and os.path.isfile(existing[name].stored_path)
-                    ]
-            return await asyncio.to_thread(_copy_broker)
-
-        if getattr(task, "workspace_kind", None) == "broker":
-            paths = await _broker_paths()
-        else:
-            paths = await _native_paths()
+        def _read_paths() -> list[str]:
+            from hermes_cli import kanban_db as _kb
+            with contextlib.closing(_kb.connect(board=board)) as conn:
+                attachments = {
+                    str(attachment.filename): str(attachment.stored_path)
+                    for attachment in _kb.list_attachments(conn, task.id)
+                }
+            return [
+                attachments[name] for name in artifact_names
+                if name in attachments and os.path.isfile(attachments[name])
+            ]
+
+        paths = await asyncio.to_thread(_read_paths)
         if not paths:
             return
 
diff --git a/gateway/run.py b/gateway/run.py
index 83341e11703fa621850354fae1dd218f385d2fe8..1d869883a439d8693eaa371ce179cbc55d0293ec 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -1110,6 +1110,7 @@ _AUTO_APPEND_MEDIA_TOOL_NAMES = {
     "text_to_speech",
     "text_to_speech_tool",
     "image_generate",
+    "kanban_attachments",
 }
 
 # ---- helpers: detect interrupted tool tails & auto-continue noise ----------
@@ -1181,6 +1182,49 @@ _TOOL_MEDIA_RE = re.compile(
 )
 
 
+def _resolve_kanban_attachment_delivery(payload: Any) -> List[str]:
+    """Resolve an opaque delivery request inside the trusted gateway process."""
+    if not isinstance(payload, dict) or not payload.get("ok"):
+        return []
+    request = payload.get("attachment_delivery")
+    if not isinstance(request, dict):
+        return []
+    board = request.get("board")
+    task_id = request.get("task_id")
+    filenames = request.get("filenames")
+    if (
+        not isinstance(board, str)
+        or not isinstance(task_id, str)
+        or not isinstance(filenames, list)
+        or not filenames
+        or any(not isinstance(name, str) for name in filenames)
+    ):
+        return []
+    try:
+        from hermes_cli import kanban_db as kb
+
+        with kb.connect(board=board) as conn:
+            task = kb.get_task(conn, task_id)
+            if task is None or task.status != "done":
+                return []
+            by_name = {
+                attachment.filename: attachment
+                for attachment in kb.list_attachments(conn, task_id)
+            }
+            selected = [by_name.get(name) for name in filenames]
+            if any(attachment is None for attachment in selected):
+                return []
+            paths = [str(Path(attachment.stored_path)) for attachment in selected]
+            if not all(Path(path).is_file() for path in paths):
+                return []
+            return paths
+    except Exception:
+        logger.exception(
+            "Failed to resolve Kanban attachment delivery for task %s", task_id
+        )
+        return []
+
+
 def _collect_auto_append_media_tags(
     messages: List[Dict[str, Any]],
     history_offset: int = 0,
@@ -1233,6 +1277,19 @@ def _collect_auto_append_media_tags(
             continue
         content = str(msg.get("content") or "")
         tool_name = tool_name_by_call_id.get(call_id)
+        if tool_name == "kanban_attachments":
+            try:
+                payload = json.loads(content)
+            except Exception:
+                payload = None
+            # An explicit re-delivery request intentionally bypasses history
+            # dedup. The tool result contains only task metadata; host paths are
+            # resolved here and never exposed to the model.
+            media_tags.extend(
+                f"MEDIA:{path}"
+                for path in _resolve_kanban_attachment_delivery(payload)
+            )
+            continue
         # JSON-payload tools (image_generate) return a local-file path in a
         # known field rather than a MEDIA: tag. Extract it so delivery is
         # deterministic even when the model omits the path from its reply.
diff --git a/plugins/workspace-service/__init__.py b/plugins/workspace-service/__init__.py
index b46ecaa7a2fe373302e61f22c044776d1fe17e6a..6cacc1ce4370ecf43612b73fec506a16727da0de 100644
--- a/plugins/workspace-service/__init__.py
+++ b/plugins/workspace-service/__init__.py
@@ -1,7 +1,8 @@
-"""Broker-backed workspace capture/import/export lifecycle hooks.
+"""Broker-backed immutable workspace capture lifecycle hooks.
 
-Hermes owns only intent rows in the existing task_events table.  The broker
-owns every activation, handoff, import, export, lease, and replay journal.
+Hermes owns completion intent and native task attachments. The broker owns each
+activation, frozen handoff, and replay journal. Broker output never becomes a
+writable child workspace.
 """
 from __future__ import annotations
 
@@ -40,16 +41,13 @@ def _opaque_id(prefix: str) -> str:
     return f"{prefix}_{secrets.token_hex(16)}"
 
 
-def _stable_delivery_id(intent_id: str, relative_path: str) -> str:
-    digest = hashlib.sha256(f"{intent_id}\x00{relative_path}".encode()).hexdigest()
+def _stable_delivery_id(finalization_id: str, relative_path: str) -> str:
+    digest = hashlib.sha256(
+        f"{finalization_id}\x00{relative_path}".encode()
+    ).hexdigest()
     return f"d_{digest[:32]}"
 
 
-def _stable_preparation_id(task_id: str, run_id: int, source_run_id: int) -> str:
-    digest = hashlib.sha256(f"{task_id}\x00{run_id}\x00{source_run_id}".encode()).hexdigest()
-    return f"p_{digest[:32]}"
-
-
 def _capture_replay(
     client: BrokerClient,
     board: str,
@@ -68,30 +66,11 @@ def _capture_replay(
     )
 
 
-def _prepare_and_release_selected(
-    client: BrokerClient, handoff_id: str, intent_id: str, artifacts: list[str]
-) -> None:
-    # Completion must prove every selected file exists in the frozen broker
-    # tree before Hermes closes the run. Tokens are intentionally released;
-    # notifier delivery prepares fresh, subscriber-specific exports later.
-    for relative_path in artifacts:
-        delivery_id = _stable_delivery_id(intent_id, relative_path)
-        prepared = client.prepare_export(
-            delivery_id=delivery_id,
-            handoff_id=handoff_id,
-            relative_path=relative_path.removeprefix("output/"),
-        )
-        try:
-            if prepared["size"] < 0:
-                raise RuntimeError("broker returned a negative export size")
-        finally:
-            client.release_export(prepared["exportToken"])
-
-
 def _on_completion_intent(
     *, task_id: str, board: str, run_id: int, finalization_id: str,
     artifacts: list[str], **_: Any,
 ) -> dict[str, bool]:
+    """Fence the producer, freeze output, and install selected native attachments."""
     if not _enabled():
         raise RuntimeError("workspace handoff is disabled")
     board = _board_name(board)
@@ -101,32 +80,52 @@ def _on_completion_intent(
             raise RuntimeError("workspace completion task is stale")
         if task.current_run_id != int(run_id):
             raise RuntimeError("workspace completion run is stale")
+
     client = _client()
     try:
         captured = _capture_replay(
             client, board, task_id, int(run_id), finalization_id, list(artifacts)
         )
-        intent = f"{finalization_id}:{task_id}:{run_id}"
-        _prepare_and_release_selected(client, captured["handoffId"], intent, list(artifacts))
+        handoff_id = str(captured["handoffId"])
+        selected = captured.get("selectedArtifacts")
+        if selected != list(artifacts):
+            raise RuntimeError("broker selected-artifact manifest changed")
+        materialized = [
+            (
+                relative_path,
+                client.read_handoff_artifact_bytes(
+                    handoff_id=handoff_id,
+                    relative_path=relative_path,
+                ),
+            )
+            for relative_path in artifacts
+        ]
     finally:
         client.close()
+
     with contextlib.closing(kb.connect(board=board)) as conn:
         kb.append_workspace_capture(
             conn, task_id, run_id=int(run_id),
-            handoff_id=str(captured["handoffId"]),
+            handoff_id=handoff_id,
             finalization_id=str(finalization_id),
             artifacts=list(artifacts),
             board=board,
         )
+        for relative_path, data in materialized:
+            kb.store_workspace_handoff_attachment(
+                conn,
+                task_id,
+                filename=relative_path,
+                data=data,
+                delivery_id=_stable_delivery_id(finalization_id, relative_path),
+                run_id=int(run_id),
+                board=board,
+            )
     return {"ready": True}
 
 
-
-
 def _authority_for_run(conn: Any, task_id: str, run_id: int) -> dict[str, Any]:
-    specification = kb.get_task_worker_specification(
-        conn, task_id, run_id=int(run_id)
-    )
+    specification = kb.get_task_worker_specification(conn, task_id, run_id=int(run_id))
     if specification is None:
         raise RuntimeError("durable worker specification is missing")
     worklane = specification.policy.get("worklane")
@@ -142,10 +141,7 @@ def _authority_for_run(conn: Any, task_id: str, run_id: int) -> dict[str, Any]:
         "policyRevision": specification.policy_revision,
     }
     if specification.project is not None:
-        if (
-            specification.project_revision is None
-            or specification.source_generation is None
-        ):
+        if specification.project_revision is None or specification.source_generation is None:
             raise RuntimeError("worker specification Project source is incomplete")
         authority.update(
             project=specification.project,
@@ -186,7 +182,9 @@ def _on_pre_spawn(*, task_id: str, board: str, run_id: int, **_: Any) -> dict[st
     return {"ready": True}
 
 
-def _on_spawn_failed(*, task_id: str, board: str, run_id: int | None = None, **_: Any) -> None:
+def _on_spawn_failed(
+    *, task_id: str, board: str, run_id: int | None = None, **_: Any
+) -> None:
     if not _enabled() or run_id is None:
         return
     try:
@@ -195,13 +193,12 @@ def _on_spawn_failed(*, task_id: str, board: str, run_id: int | None = None, **_
             client.consume_task_run(
                 board=board,
                 environment_key=broker_environment_key(task_id),
-                task_id=task_id, run_id=int(run_id),
+                task_id=task_id,
+                run_id=int(run_id),
             )
         finally:
             client.close()
     except Exception:
-        # The broker activation remains durable and its reclaim/TTL path is the
-        # authority if a local failure prevents immediate consumption.
         return
 
 
@@ -209,33 +206,21 @@ def _on_terminal_workspace(
     *, task_id: str, board: str, run_id: int | None = None, **_: Any
 ) -> None:
     """Consume a broker activation after a terminal worker transition."""
-    if not _enabled() or run_id is None:
-        return
-    try:
-        client = _client()
-        try:
-            client.consume_task_run(
-                board=board,
-                environment_key=broker_environment_key(task_id),
-                task_id=task_id,
-                run_id=int(run_id),
-            )
-        finally:
-            client.close()
-    except Exception:
-        # The broker's durable lease/reclaim path remains authoritative if
-        # notification or process teardown cannot reach it immediately.
-        return
+    _on_spawn_failed(task_id=task_id, board=board, run_id=run_id)
 
 
-def _on_recovery(*, task_id: str, board: str, run_id: int, **_: Any) -> dict[str, bool]:
+def _on_recovery(
+    *, task_id: str, board: str, run_id: int, **_: Any
+) -> dict[str, bool]:
     board = _board_name(board)
     with contextlib.closing(kb.connect(board=board)) as conn:
         completion_intent = kb.get_completion_intent(conn, task_id, int(run_id))
     if not isinstance(completion_intent, dict) or not completion_intent.get("finalization_id"):
         raise RuntimeError("workspace recovery intent is unavailable")
     return _on_completion_intent(
-        task_id=task_id, board=board, run_id=int(run_id),
+        task_id=task_id,
+        board=board,
+        run_id=int(run_id),
         finalization_id=str(completion_intent["finalization_id"]),
         artifacts=list(completion_intent.get("artifacts", [])),
     )
diff --git a/tests/gateway/test_media_extraction.py b/tests/gateway/test_media_extraction.py
index 65d4a72a2f0ef0b54a577acbc58001d9c951b25a..5a2fde299c56f6788efce8a45184af2e7d5ebbea 100644
--- a/tests/gateway/test_media_extraction.py
+++ b/tests/gateway/test_media_extraction.py
@@ -185,6 +185,74 @@ caption
         assert tags == ["MEDIA:/tmp/gen/cat.png"]
         assert voice is False
 
+    def test_gateway_resolves_opaque_completed_attachment_delivery(
+        self, tmp_path, monkeypatch
+    ):
+        """Task tool results keep host paths hidden while the gateway delivers."""
+        import json
+        import time
+
+        monkeypatch.setenv("HERMES_HOME", str(tmp_path / ".hermes"))
+        from hermes_cli import kanban_db as kb
+        from gateway.run import _collect_auto_append_media_tags
+
+        kb.init_db()
+        conn = kb.connect(board="delivery-board")
+        try:
+            task_id = kb.create_task(
+                conn,
+                title="deliver",
+                assignee="worker",
+                board="delivery-board",
+            )
+            kb.store_attachment_bytes(
+                conn,
+                task_id,
+                "output/result.json",
+                b'{"result":"ok"}',
+                content_type="application/json",
+                uploaded_by="workspace_handoff",
+                board="delivery-board",
+            )
+            attachment = kb.list_attachments(conn, task_id)[0]
+            conn.execute(
+                "UPDATE tasks SET status = 'done', completed_at = ? WHERE id = ?",
+                (int(time.time()), task_id),
+            )
+        finally:
+            conn.close()
+
+        payload = {
+            "ok": True,
+            "attachment_delivery": {
+                "board": "delivery-board",
+                "task_id": task_id,
+                "filenames": [attachment.filename],
+            },
+        }
+        content = json.dumps(payload)
+        assert attachment.stored_path not in content
+        messages = [
+            {
+                "role": "assistant",
+                "tool_calls": [{
+                    "id": "call_delivery",
+                    "function": {"name": "kanban_attachments"},
+                }],
+            },
+            {
+                "role": "tool",
+                "tool_call_id": "call_delivery",
+                "content": content,
+            },
+            {"role": "assistant", "content": "Attached."},
+        ]
+
+        tags, voice = _collect_auto_append_media_tags(messages)
+
+        assert tags == [f"MEDIA:{attachment.stored_path}"]
+        assert voice is False
+
     def test_gateway_auto_append_image_generate_prefers_host_path(self):
         """When host and sandbox paths differ, the host-deliverable path wins."""
         from gateway.run import _collect_auto_append_media_tags
diff --git a/tests/plugins/test_workspace_zero_schema.py b/tests/plugins/test_workspace_zero_schema.py
index 6df64c787c99821a124c5d3436306eb3d1787661..5b5eec777c6fcab3afb33f7a235b703564be06ca 100644
--- a/tests/plugins/test_workspace_zero_schema.py
+++ b/tests/plugins/test_workspace_zero_schema.py
@@ -41,6 +41,76 @@ def test_broker_workspace_uses_existing_schema_and_hides_intent(tmp_path, monkey
         assert not forbidden & tables
 
 
+def test_parent_dependency_gets_distinct_broker_workspace(tmp_path, monkeypatch):
+    home = tmp_path / ".hermes"
+    monkeypatch.setenv("HERMES_HOME", str(home))
+    monkeypatch.setenv("HERMES_WORKSPACE_HANDOFF", "1")
+    monkeypatch.setenv("HERMES_SANDBOX_AUTHORITY_BINDING", "parent-isolation")
+    kb.init_db()
+    with kb.connect(board="default") as conn:
+        parent_id = kb.create_task(
+            conn,
+            title="parent",
+            assignee="worker",
+            workspace_kind="scratch",
+        )
+        kb.claim_task(conn, parent_id)
+        assert kb.complete_task(
+            conn, parent_id, summary="parent complete", artifacts=[]
+        )
+        child_id = kb.create_task(
+            conn,
+            title="dependent child",
+            assignee="worker",
+            parents=[parent_id],
+            workspace_kind="broker",
+        )
+        child = kb.get_task(conn, child_id)
+        assert child is not None and child.workspace_path is None
+        kb.claim_task(conn, child_id)
+        run_id = int(kb.get_task(conn, child_id).current_run_id)
+
+    plugin_path = (
+        Path(__file__).parents[2] / "plugins" / "workspace-service" / "__init__.py"
+    )
+    spec = importlib.util.spec_from_file_location(
+        "workspace_service_parent_isolation_test", plugin_path
+    )
+    assert spec and spec.loader
+    module = importlib.util.module_from_spec(spec)
+    spec.loader.exec_module(module)
+
+    class Client:
+        acquired = []
+        activated = []
+        closed = False
+
+        def acquire_workspace(self, environment_key):
+            self.acquired.append(environment_key)
+            return {"workspaceId": "ws-child", "workspaceLeaseId": "lease-child"}
+
+        def activate_task_run(self, **kwargs):
+            self.activated.append(kwargs)
+
+        def close(self):
+            self.closed = True
+
+    client = Client()
+    monkeypatch.setattr(module, "_client", lambda: client)
+    monkeypatch.setattr(module, "_authority_for_run", lambda *_args: {"lane": "worker"})
+
+    assert module._on_pre_spawn(
+        task_id=child_id,
+        board="default",
+        run_id=run_id,
+    ) == {"ready": True}
+    child_key = module.broker_environment_key(child_id)
+    assert client.acquired == [child_key]
+    assert client.activated[0]["environment_key"] == child_key
+    assert child_key != module.broker_environment_key(parent_id)
+    assert client.closed is True
+
+
 def test_native_artifacts_are_relative_and_attachment_backed(tmp_path, monkeypatch):
     home = tmp_path / ".hermes"
     workspace = tmp_path / "work"
@@ -91,6 +161,52 @@ def test_completion_intent_replay_excludes_reclaim_and_dry_run(tmp_path, monkeyp
         assert kb.get_task(conn, task_id).status == "done"
 
 
+def test_broker_completion_materializes_selected_attachment_before_done(tmp_path, monkeypatch):
+    monkeypatch.setenv("HERMES_HOME", str(tmp_path / ".hermes"))
+    monkeypatch.setenv("HERMES_WORKSPACE_HANDOFF", "1")
+    monkeypatch.setenv("HERMES_SANDBOX_AUTHORITY_BINDING", "capture-binding")
+    kb.init_db()
+    plugin_path = Path(__file__).parents[2] / "plugins" / "workspace-service" / "__init__.py"
+    spec = importlib.util.spec_from_file_location("workspace_service_capture_test", plugin_path)
+    assert spec and spec.loader
+    module = importlib.util.module_from_spec(spec)
+    spec.loader.exec_module(module)
+
+    class CaptureClient:
+        def capture_handoff(self, **kwargs):
+            return {
+                "handoffId": "00000000-0000-4000-8000-000000000001",
+                "selectedArtifacts": list(kwargs["selected_artifacts"]),
+            }
+
+        def read_handoff_artifact_bytes(self, **kwargs):
+            assert kwargs["relative_path"] == "output/report.txt"
+            return b"frozen bytes"
+
+        def close(self):
+            return None
+
+    monkeypatch.setattr(module, "_client", CaptureClient)
+    with kb.connect(board="capture-board") as conn:
+        task_id = kb.create_task(
+            conn, title="capture", assignee="worker", workspace_kind="broker"
+        )
+        kb.claim_task(conn, task_id)
+        run_id = int(kb.get_task(conn, task_id).current_run_id)
+    assert module._on_completion_intent(
+        task_id=task_id,
+        board="capture-board",
+        run_id=run_id,
+        finalization_id="f_capture",
+        artifacts=["output/report.txt"],
+    ) == {"ready": True}
+    with kb.connect(board="capture-board") as conn:
+        task = kb.get_task(conn, task_id)
+        attachments = kb.list_attachments(conn, task_id)
+        assert task.status == "running"
+        assert len(attachments) == 1
+        assert attachments[0].filename == "output/report.txt"
+        assert Path(attachments[0].stored_path).read_bytes() == b"frozen bytes"
 
 
 def test_terminal_hook_failure_is_best_effort(tmp_path, monkeypatch):
diff --git a/tests/tools/test_kanban_role_boundaries.py b/tests/tools/test_kanban_role_boundaries.py
index 5a5ff8bbed825d7488ab5f30edee796fc5bc7f82..9667d204c1aef16434f53c0f718a15f4f846d864 100644
--- a/tests/tools/test_kanban_role_boundaries.py
+++ b/tests/tools/test_kanban_role_boundaries.py
@@ -59,8 +59,17 @@ def test_kanban_orchestrator_guidance_forbids_worker_substitution():
     assert "Do not execute a task body yourself" in KANBAN_ORCHESTRATOR_GUIDANCE
     assert "substitute `delegate_task`" in KANBAN_ORCHESTRATOR_GUIDANCE
     assert "A `ready` or `running` task is queued" in KANBAN_ORCHESTRATOR_GUIDANCE
-    assert "Short tasks need not emit heartbeats" in KANBAN_ORCHESTRATOR_GUIDANCE
+    assert "Completion notifications are push-driven" in KANBAN_ORCHESTRATOR_GUIDANCE
     assert "Do not create a replacement" in KANBAN_ORCHESTRATOR_GUIDANCE
+    assert "downloadable artifact or durable task is board work" in (
+        KANBAN_ORCHESTRATOR_GUIDANCE
+    )
+    assert "Never recreate, base64-encode, execute code for" in (
+        KANBAN_ORCHESTRATOR_GUIDANCE
+    )
+    assert '`kanban_attachments` with `action="deliver"`' in (
+        KANBAN_ORCHESTRATOR_GUIDANCE
+    )
     assert "Worker lifecycle belongs to the assigned worker" in (
         KANBAN_ORCHESTRATOR_GUIDANCE
     )
diff --git a/tests/tools/test_kanban_tools.py b/tests/tools/test_kanban_tools.py
index 91292f372910b3ec601118e827784bdb8bf5dbe6..f67842fa4eec94ca254ce88837d019378d481197 100644
--- a/tests/tools/test_kanban_tools.py
+++ b/tests/tools/test_kanban_tools.py
@@ -9,6 +9,7 @@ from __future__ import annotations
 
 import json
 import os
+import time
 
 import pytest
 
@@ -2146,6 +2147,8 @@ def test_board_param_in_all_schemas():
         )
 
 
+
+
 # ---------------------------------------------------------------------------
 # kanban_create auto-subscribe behaviour
 #
@@ -2484,6 +2487,84 @@ def test_attachments_unknown_task_errors(worker_env):
     assert "error" in json.loads(out)
 
 
+def test_completed_broker_attachment_is_delivered_without_exposing_host_path(
+    worker_env, tmp_path,
+):
+    from hermes_cli import kanban_db as kb
+    from tools import kanban_tools as kt
+
+    conn = kb.connect()
+    try:
+        kb.store_attachment_bytes(
+            conn,
+            worker_env,
+            "output/report.json",
+            b'{"ready":true}',
+            content_type="application/json",
+            uploaded_by="workspace_handoff",
+        )
+        kb.store_attachment_bytes(
+            conn,
+            worker_env,
+            "manual-note.txt",
+            b"manual",
+            content_type="text/plain",
+            uploaded_by="agent",
+        )
+        conn.execute(
+            "UPDATE tasks SET status = 'done', completed_at = ? WHERE id = ?",
+            (int(time.time()), worker_env),
+        )
+    finally:
+        conn.close()
+
+    result = json.loads(kt._handle_attachments({"action": "deliver"}))
+
+    assert result["ok"] is True
+    assert result["attachment_delivery"]["task_id"] == worker_env
+    assert result["attachment_delivery"]["filenames"] == ["report.json"]
+    assert "stored_path" not in json.dumps(result)
+    assert str(tmp_path) not in json.dumps(result)
+
+
+def test_completed_broker_attachment_cannot_be_reuploaded(worker_env):
+    import base64
+
+    from hermes_cli import kanban_db as kb
+    from tools import kanban_tools as kt
+
+    payload = b"immutable handoff bytes"
+    conn = kb.connect()
+    try:
+        kb.store_attachment_bytes(
+            conn,
+            worker_env,
+            "output/report.txt",
+            payload,
+            content_type="text/plain",
+            uploaded_by="workspace_handoff",
+        )
+        conn.execute(
+            "UPDATE tasks SET status = 'done', completed_at = ? WHERE id = ?",
+            (int(time.time()), worker_env),
+        )
+    finally:
+        conn.close()
+
+    result = json.loads(kt._handle_attach({
+        "filename": "renamed-copy.txt",
+        "content_base64": base64.b64encode(payload).decode(),
+    }))
+
+    assert "error" in result
+    assert "already present from broker completion" in result["error"]
+    conn = kb.connect()
+    try:
+        assert len(kb.list_attachments(conn, worker_env)) == 1
+    finally:
+        conn.close()
+
+
 def test_attach_url_fetches_local_fixture(worker_env, allow_private_urls):
     """kanban_attach_url downloads from an http(s) URL and stores the bytes.
 
diff --git a/tools/environments/gondolin.py b/tools/environments/gondolin.py
index cdb16ceb43921c72a7e9d1e2193a1bb389080812..343b8d5efc41d877f9359902f2ed9f102cd452c1 100644
--- a/tools/environments/gondolin.py
+++ b/tools/environments/gondolin.py
@@ -207,54 +207,9 @@ class BrokerClient:
             "handoffId": self._required_text(body, "handoffId"),
             "entryCount": int(body.get("entryCount", 0)),
             "totalBytes": int(body.get("totalBytes", 0)),
+            "selectedArtifacts": list(body.get("selectedArtifacts", [])),
         }
 
-    def import_handoff(
-        self,
-        *,
-        preparation_id: str,
-        board: str,
-        source_handoff_id: str,
-        source_task_id: str,
-        destination_task_id: str,
-        destination_run_id: int,
-        destination_environment_key: str,
-    ) -> dict:
-        destination_run = broker_task_run(
-            board, destination_task_id, destination_run_id
-        )
-        body = self._json_post(
-            "/v1/control/workspace-handoffs/import",
-            {
-                "preparationId": str(preparation_id),
-                "sourceHandoffId": str(source_handoff_id),
-                "sourceTaskId": broker_task_identity(board, source_task_id),
-                "destinationTaskId": destination_run["taskId"],
-                "destinationRunId": destination_run["runId"],
-                "destinationEnvironmentKey": str(destination_environment_key),
-            },
-            timeout=max(self._timeout, 180.0),
-        )
-        workspace = body.get("workspace")
-        lease = body.get("lease")
-        if (
-            not isinstance(workspace, dict)
-            or not isinstance(lease, dict)
-            or workspace.get("guestPath") != "/workspace"
-        ):
-            raise BrokerError(
-                "protocol.invalid_response", "broker import binding is invalid"
-            )
-        return {
-            "preparationId": self._required_text(body, "preparationId"),
-            "sourceHandoffId": self._required_text(body, "sourceHandoffId"),
-            "workspaceId": self._required_text(workspace, "workspaceId"),
-            "ownerEnvironmentKey": self._required_text(
-                workspace, "ownerEnvironmentKey"
-            ),
-            "workspaceLeaseId": self._required_text(lease, "leaseId"),
-            "guestPath": "/workspace",
-        }
 
     def acquire_workspace(
         self, environment_key: str, *, workspace_id: str | None = None
@@ -329,49 +284,19 @@ class BrokerClient:
                 "protocol.invalid_response", "broker activation was not consumed"
             )
 
-    def prepare_export(
-        self, *, delivery_id: str, handoff_id: str, relative_path: str
-    ) -> dict:
-        body = self._json_post(
-            "/v1/control/workspace-handoffs/exports/prepare",
-            {
-                "deliveryId": str(delivery_id),
-                "handoffId": str(handoff_id),
-                "relativePath": str(relative_path),
-            },
-        )
-        token = self._required_text(body, "exportToken")
-        name = self._required_text(body, "fileName")
-        size = body.get("size")
-        expires = body.get("expiresAt")
-        if (
-            not isinstance(size, int)
-            or size < 0
-            or not isinstance(expires, int)
-            or expires <= 0
-        ):
-            raise BrokerError(
-                "protocol.invalid_response", "broker export metadata is invalid"
-            )
-        return {
-            "exportToken": token,
-            "fileName": name,
-            "size": size,
-            "expiresAt": expires,
-        }
-
-    def read_export_bytes(
+    def read_handoff_artifact_bytes(
         self,
-        export_token: str,
         *,
-        expected_size: int,
+        handoff_id: str,
+        relative_path: str,
         max_bytes: int = 25 * 1024 * 1024,
     ) -> bytes:
-        if expected_size < 0 or expected_size > max_bytes:
-            raise BrokerError("policy.exceeded", "export exceeds attachment limit")
         response = self._client.post(
-            "/v1/control/workspace-handoffs/exports/read",
-            json={"exportToken": str(export_token)},
+            "/v1/control/workspace-handoffs/artifacts/read",
+            json={
+                "handoffId": str(handoff_id),
+                "relativePath": str(relative_path),
+            },
             timeout=self._timeout,
             headers={
                 "accept": "application/octet-stream",
@@ -380,27 +305,17 @@ class BrokerClient:
         )
         self._raise_for_status(response)
         content_length = response.headers.get("content-length")
-        if content_length is not None and int(content_length) != expected_size:
-            raise BrokerError(
-                "protocol.invalid_response", "export content length mismatch"
-            )
+        if content_length is not None and int(content_length) > max_bytes:
+            raise BrokerError("policy.exceeded", "artifact exceeds attachment limit")
         data = response.content
-        if len(data) != expected_size:
+        if len(data) > max_bytes:
+            raise BrokerError("policy.exceeded", "artifact exceeds attachment limit")
+        if content_length is not None and len(data) != int(content_length):
             raise BrokerError(
-                "protocol.invalid_response", "export byte count mismatch"
+                "protocol.invalid_response", "artifact content length mismatch"
             )
         return bytes(data)
 
-    def release_export(self, export_token: str) -> None:
-        body = self._json_post(
-            "/v1/control/workspace-handoffs/exports/release",
-            {"exportToken": str(export_token)},
-        )
-        if body.get("released") is not True:
-            raise BrokerError(
-                "protocol.invalid_response", "broker export was not released"
-            )
-
 
     def health(self) -> bool:
         response = self._client.get("/v1/health", timeout=self._timeout)
diff --git a/tools/kanban_tools.py b/tools/kanban_tools.py
index 1f66aabbab03f907f4bf3b933c512f20dda3a871..84b1ab7b138e0dbcf88fbab90d55d4fb176c8eef 100644
--- a/tools/kanban_tools.py
+++ b/tools/kanban_tools.py
@@ -30,8 +30,10 @@ from __future__ import annotations
 
 import copy
 import json
+import hashlib
 import logging
 import os
+from pathlib import Path
 from typing import Any, Optional
 
 from agent.redact import redact_sensitive_text
@@ -882,6 +884,45 @@ def _handle_comment(args: dict, **kw) -> str:
         return tool_error(f"kanban_comment: {e}")
 
 
+def _matching_completed_attachment(kb, conn, task_id: str, filename: str, data: bytes):
+    """Return a broker-produced attachment this upload would duplicate."""
+    task = kb.get_task(conn, task_id)
+    if task is None:
+        return None
+    candidates = [
+        attachment
+        for attachment in kb.list_attachments(conn, task_id)
+        if (attachment.uploaded_by or "") == "workspace_handoff"
+    ]
+    if not candidates:
+        return None
+    leaf = Path(filename).name
+    digest = None
+    for attachment in candidates:
+        if Path(attachment.filename).name == leaf:
+            return attachment
+        if attachment.size != len(data):
+            continue
+        if digest is None:
+            digest = hashlib.sha256(data).digest()
+        try:
+            with Path(attachment.stored_path).open("rb") as stored:
+                stored_digest = hashlib.file_digest(stored, "sha256").digest()
+        except OSError:
+            continue
+        if stored_digest == digest:
+            return attachment
+    return None
+
+
+def _duplicate_completion_error(filename: str) -> str:
+    return tool_error(
+        f"attachment {filename!r} is already present from broker completion; "
+        "do not upload a second copy. Use kanban_attachments with "
+        "action='deliver' only when the human explicitly asks to receive it again."
+    )
+
+
 def _handle_attach(args: dict, **kw) -> str:
     """Attach an inline (base64) file to a task.
 
@@ -919,6 +960,11 @@ def _handle_attach(args: dict, **kw) -> str:
     try:
         _, conn = _connect(board=board)
         try:
+            duplicate = _matching_completed_attachment(
+                kb, conn, tid, str(filename), data
+            )
+            if duplicate is not None:
+                return _duplicate_completion_error(duplicate.filename)
             att_id = kb.store_attachment_bytes(
                 conn,
                 tid,
@@ -1045,6 +1091,11 @@ def _handle_attach_url(args: dict, **kw) -> str:
     try:
         _, conn = _connect(board=board)
         try:
+            duplicate = _matching_completed_attachment(
+                kb, conn, tid, str(filename), data
+            )
+            if duplicate is not None:
+                return _duplicate_completion_error(duplicate.filename)
             att_id = kb.store_attachment_bytes(
                 conn,
                 tid,
@@ -1067,36 +1118,86 @@ def _handle_attach_url(args: dict, **kw) -> str:
 
 
 def _handle_attachments(args: dict, **kw) -> str:
-    """List a task's attachments (read-only; no ownership restriction)."""
+    """List task attachments or request native delivery of existing files."""
     tid = _default_task_id(args.get("task_id"))
     if not tid:
         return tool_error(
             "task_id is required (or set HERMES_KANBAN_TASK in the env)"
         )
     board = args.get("board")
+    action = args.get("action") or "list"
     try:
         kb, conn = _connect(board=board)
         try:
-            if kb.get_task(conn, tid) is None:
+            task = kb.get_task(conn, tid)
+            if task is None:
                 return tool_error(f"task {tid} not found")
-            atts = kb.list_attachments(conn, tid)
-            return json.dumps({
-                "ok": True,
-                "task_id": tid,
-                "attachments": [
-                    {
-                        "filename": a.filename,
-                        "content_type": a.content_type,
-                        "size": a.size,
-                        "uploaded_by": (
-                            "hermes-system" if (a.uploaded_by or "").startswith("kanban_")
-                            else a.uploaded_by
-                        ),
-                        "created_at": a.created_at,
-                    }
-                    for a in atts
-                ],
-            })
+            attachments = kb.list_attachments(conn, tid)
+            if action == "list":
+                return json.dumps({
+                    "ok": True,
+                    "task_id": tid,
+                    "attachments": [
+                        {
+                            "filename": attachment.filename,
+                            "content_type": attachment.content_type,
+                            "size": attachment.size,
+                            "uploaded_by": (
+                                "hermes-system"
+                                if (attachment.uploaded_by or "").startswith("kanban_")
+                                else attachment.uploaded_by
+                            ),
+                            "created_at": attachment.created_at,
+                        }
+                        for attachment in attachments
+                    ],
+                })
+            if action != "deliver":
+                return tool_error(f"unsupported attachment action {action!r}")
+            if task.status != "done":
+                return tool_error(
+                    f"task {tid} is {task.status}; attachments can be delivered "
+                    "only after authoritative completion"
+                )
+            requested = args.get("filenames")
+            if requested is not None:
+                if not requested:
+                    return tool_error(
+                        "filenames must contain at least one exact attachment name"
+                    )
+                by_name = {
+                    attachment.filename: attachment
+                    for attachment in attachments
+                }
+                missing = [
+                    str(name) for name in requested if str(name) not in by_name
+                ]
+                if missing:
+                    return tool_error(
+                        "attachments not found: " + ", ".join(sorted(set(missing)))
+                    )
+                selected = [by_name[str(name)] for name in requested]
+            else:
+                completed = [
+                    attachment
+                    for attachment in attachments
+                    if (attachment.uploaded_by or "") == "workspace_handoff"
+                ]
+                selected = completed or attachments
+            if not selected:
+                return tool_error(f"task {tid} has no attachments")
+            resolved_board = (
+                board
+                or os.environ.get("HERMES_KANBAN_BOARD")
+                or kb.get_current_board()
+            )
+            return _ok(
+                attachment_delivery={
+                    "board": resolved_board,
+                    "task_id": tid,
+                    "filenames": [attachment.filename for attachment in selected],
+                }
+            )
         finally:
             conn.close()
     except ValueError as e:
@@ -1106,6 +1207,8 @@ def _handle_attachments(args: dict, **kw) -> str:
         return tool_error(f"kanban_attachments: {e}")
 
 
+
+
 def _handle_create(args: dict, **kw) -> str:
     """Create a child task. Orchestrator workers use this to fan out.
 
@@ -1739,12 +1842,12 @@ KANBAN_COMMENT_SCHEMA = {
 KANBAN_ATTACH_SCHEMA = {
     "name": "kanban_attach",
     "description": (
-        "Attach a file to a task by passing its bytes inline (base64). "
-        "Use for genuine file artifacts the next worker or a human should "
-        "be able to download — generated reports, images, exports. The "
-        "file is stored as a real attachment (not a comment link) under "
-        "the task's attachments dir, capped at 25 MB. Prefer "
-        "kanban_attach_url when you only have a URL."
+        "Attach a new file to an active task by passing its bytes inline "
+        "(base64). Use for genuine artifacts the next worker or a human should "
+        "be able to download. Never recreate or re-upload an artifact already "
+        "materialized by broker completion; use kanban_attachments with "
+        "action='deliver' only on an explicit human request. Stored as "
+        "a real attachment, capped at 25 MB. Prefer kanban_attach_url for a URL."
     ),
     "parameters": {
         "type": "object",
@@ -1777,10 +1880,10 @@ KANBAN_ATTACH_SCHEMA = {
 KANBAN_ATTACH_URL_SCHEMA = {
     "name": "kanban_attach_url",
     "description": (
-        "Attach a file to a task by URL — Hermes downloads it server-side "
-        "and stores it as a real attachment (capped at 25 MB). Use when "
-        "you have a link rather than the bytes. Only http/https URLs are "
-        "accepted."
+        "Attach a new file to an active task by URL; Hermes downloads it "
+        "server-side and stores it as a real attachment (capped at 25 MB). "
+        "Never use this to duplicate a broker-completion artifact. Only "
+        "http/https URLs are accepted."
     ),
     "parameters": {
         "type": "object",
@@ -1816,8 +1919,11 @@ KANBAN_ATTACH_URL_SCHEMA = {
 KANBAN_ATTACHMENTS_SCHEMA = {
     "name": "kanban_attachments",
     "description": (
-        "List metadata for files attached to a task: filename, content_type, "
-        "size, and uploader. Internal row ids and host storage paths are hidden."
+        "List metadata for files attached to a task, or explicitly deliver "
+        "existing attachments from an already completed task through the "
+        "current native chat transport. Internal row ids and host storage paths "
+        "are always hidden. Use action='deliver' only when the human explicitly "
+        "asks to receive or download existing files again."
     ),
     "parameters": {
         "type": "object",
@@ -1826,12 +1932,32 @@ KANBAN_ATTACHMENTS_SCHEMA = {
                 "type": "string",
                 "description": _DESC_TASK_ID_DEFAULT,
             },
+            "action": {
+                "type": "string",
+                "enum": ["list", "deliver"],
+                "description": (
+                    "Defaults to 'list'. 'deliver' sends existing completed-task "
+                    "attachments without recreating their bytes."
+                ),
+            },
+            "filenames": {
+                "type": "array",
+                "items": {"type": "string"},
+                "minItems": 1,
+                "uniqueItems": True,
+                "description": (
+                    "With action='deliver', optional exact filenames. Omit to "
+                    "deliver broker-completion artifacts, or all attachments "
+                    "when none came from broker completion."
+                ),
+            },
             "board": _board_schema_prop(),
         },
         "required": [],
     },
 }
 
+
 KANBAN_CREATE_SCHEMA = {
     "name": "kanban_create",
     "description": (
@@ -2063,7 +2189,6 @@ def _kanban_create_schema_overrides() -> dict:
         name: dict(schema)
         for name, schema in KANBAN_CREATE_SCHEMA["parameters"]["properties"].items()
     }
-
     assignee = parameters["properties"]["assignee"]
     assignee["description"] = (
         assignee["description"]
@@ -2204,6 +2329,7 @@ registry.register(
     emoji="📎",
 )
 
+
 registry.register(
     name="kanban_create",
     toolset="kanban",
