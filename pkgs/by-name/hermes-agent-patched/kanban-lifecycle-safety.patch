diff --git a/agent/agent_init.py b/agent/agent_init.py
index a8773dd..28729fb 100644
--- a/agent/agent_init.py
+++ b/agent/agent_init.py
@@ -1232,15 +1232,13 @@ def init_agent(
     elif not agent.quiet_mode:
         print("🛠️  No tools loaded (all tools filtered out or unavailable)")
 
-    # Kanban worker/orchestrator lifecycle guidance is session-static:
-    # the dispatcher decides at spawn time whether this process is a kanban
-    # worker (kanban_show tool is present iff HERMES_KANBAN_TASK is set).
-    # Resolving the ~835-token block once here avoids re-running the
-    # membership test + reference on every system-prompt rebuild
-    # (init + each context compression).
-    from agent.prompt_builder import KANBAN_GUIDANCE
-    agent._kanban_worker_guidance = (
-        KANBAN_GUIDANCE if "kanban_show" in agent.valid_tool_names else ""
+    # Kanban role guidance is session-static. The dispatcher provides the
+    # trusted task identity for workers; kanban_list is orchestrator-only.
+    # Resolve the contract once here so prompt rebuilds remain byte-stable.
+    from agent.prompt_builder import select_kanban_guidance
+    agent._kanban_worker_guidance = select_kanban_guidance(
+        task_id=os.environ.get("HERMES_KANBAN_TASK"),
+        tool_names=agent.valid_tool_names,
     )
 
     # Check tool requirements
diff --git a/agent/conversation_loop.py b/agent/conversation_loop.py
index 501e49b..58bd5a3 100644
--- a/agent/conversation_loop.py
+++ b/agent/conversation_loop.py
@@ -5058,7 +5058,9 @@ def run_conversation(
                     except Exception:
                         pass
 
-                agent._execute_tool_calls(assistant_message, messages, effective_task_id, api_call_count)
+                _kanban_terminal = agent._execute_tool_calls(
+                    assistant_message, messages, effective_task_id, api_call_count
+                )
 
                 if agent._tool_guardrail_halt_decision is not None:
                     decision = agent._tool_guardrail_halt_decision
@@ -5083,6 +5085,13 @@ def run_conversation(
                                 pass
                     break
 
+                if _kanban_terminal:
+                    # The durable board transition succeeded. Do not make a
+                    # follow-up model request under the now-terminal run.
+                    _turn_exit_reason = "kanban_terminal"
+                    final_response = assistant_message.content or ""
+                    break
+
                 # Reset per-turn retry counters after successful tool
                 # execution so a single truncation doesn't poison the
                 # entire conversation.
diff --git a/agent/prompt_builder.py b/agent/prompt_builder.py
index 892fe77..0ca1727 100644
--- a/agent/prompt_builder.py
+++ b/agent/prompt_builder.py
@@ -240,6 +240,9 @@ KANBAN_GUIDANCE = (
     "reviewer can approve+unblock or request changes. Reviewing-then-"
     "completing is more honest than auto-completing work that still needs "
     "eyes on it.\n"
+    "A rejected `kanban_complete` call does not end the task: correct a safely "
+    "recoverable request, or call `kanban_block` once with the exact unresolved "
+    "error and stop.\n"
     "6. **If follow-up work appears, create it; don't do it.** Use "
     "`kanban_create(title=..., assignee=<right-worker>, parents=[your-task-id])` "
     "to spawn a child task for the appropriate specialist worker instead of "
@@ -299,6 +302,41 @@ KANBAN_GUIDANCE = (
     "cross-agent handoffs that outlive one API loop."
 )
 
+
+KANBAN_ORCHESTRATOR_GUIDANCE = (
+    "# Kanban orchestration protocol\n"
+    "You route durable work on the shared Kanban board; you are not a "
+    "dispatcher-assigned task worker.\n"
+    "1. Use the `kanban_*` tools to create, inspect, link, comment on, and "
+    "observe tasks. Do not execute a task body yourself or substitute "
+    "`delegate_task` for board work the user requested.\n"
+    "2. Create each intended task once. A `ready` or `running` task is queued "
+    "or executing, not evidence that the dispatcher is unavailable or the "
+    "worker is stalled. Short tasks need not emit heartbeats. Do not create a "
+    "replacement merely because status or heartbeat is unchanged within the "
+    "claim lease. Judge progress from authoritative task events, not a terminal "
+    "process view.\n"
+    "3. Worker lifecycle belongs to the assigned worker. Do not complete, "
+    "block, or heartbeat on its behalf. Unblock only when the task contract "
+    "or a human authorizes a retry and the recorded blocker is resolved.\n"
+    "4. Report outcomes from authoritative task state. Claim success only when "
+    "the required tasks are `done`; report a blocker instead of working around "
+    "it in the live session.\n"
+    "5. Use `kanban_comment` for relevant routing context. Never put secrets, "
+    "tokens, or raw PII in board data; task history is durable.\n"
+)
+
+
+def select_kanban_guidance(
+    *, task_id: str | None, tool_names: set[str]
+) -> str:
+    """Select the session-static Kanban contract from trusted role signals."""
+    if task_id:
+        return KANBAN_GUIDANCE
+    if "kanban_list" in tool_names:
+        return KANBAN_ORCHESTRATOR_GUIDANCE
+    return ""
+
 TOOL_USE_ENFORCEMENT_GUIDANCE = (
     "# Tool-use enforcement\n"
     "You MUST use your tools to take action — do not describe what you would do "
diff --git a/agent/system_prompt.py b/agent/system_prompt.py
index aab34cb..a6628d3 100644
--- a/agent/system_prompt.py
+++ b/agent/system_prompt.py
@@ -226,10 +226,8 @@ def build_system_prompt_parts(agent: Any, system_message: Optional[str] = None)
         tool_guidance.append(SESSION_SEARCH_GUIDANCE)
     if "skill_manage" in agent.valid_tool_names:
         tool_guidance.append(SKILLS_GUIDANCE)
-    # Kanban worker/orchestrator lifecycle — only present when the
-    # dispatcher spawned this process (kanban_show check_fn gates on
-    # HERMES_KANBAN_TASK env var). Normal chat sessions never see
-    # this block. Resolved once at __init__ (see _kanban_worker_guidance).
+    # Kanban worker/orchestrator contract, selected once during agent_init from
+    # the role-specific tool surface and trusted dispatcher task identity.
     _kanban_guidance = getattr(agent, "_kanban_worker_guidance", None)
     if _kanban_guidance:
         tool_guidance.append(_kanban_guidance)
@@ -276,7 +274,14 @@ def build_system_prompt_parts(agent: Any, system_message: Optional[str] = None)
             # "auto" or any unrecognised value — use hardcoded defaults
             model_lower = (agent.model or "").lower()
             _inject = any(p in model_lower for p in TOOL_USE_ENFORCEMENT_MODELS)
-        if _inject:
+        # Live orchestrators must be able to wait for asynchronous workers.
+        # The generic "keep working until complete" block otherwise tells
+        # tool-eager models to poll or execute child work themselves.
+        _live_kanban_orchestrator = (
+            "kanban_list" in agent.valid_tool_names
+            and "kanban_complete" not in agent.valid_tool_names
+        )
+        if _inject and not _live_kanban_orchestrator:
             stable_parts.append(TOOL_USE_ENFORCEMENT_GUIDANCE)
             _model_lower = (agent.model or "").lower()
             # Google model operational guidance (conciseness, absolute
diff --git a/agent/tool_executor.py b/agent/tool_executor.py
index f740fed..3dce9ce 100644
--- a/agent/tool_executor.py
+++ b/agent/tool_executor.py
@@ -324,7 +324,7 @@ def _run_agent_tool_execution_middleware(
     return result, observed_args
 
 
-def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0, *, finalize: bool = True) -> None:
+def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0, *, finalize: bool = True) -> bool:
     """Execute multiple tool calls concurrently using a thread pool.
 
     Results are collected in the original tool-call order and appended to
@@ -340,6 +340,7 @@ def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effe
     # Resolve the context-scaled tool-output budget once per turn (cheap, but
     # avoids rebuilding it per result inside the loop below).
     _tool_budget = _budget_for_agent(agent)
+    execution_env = get_active_env(effective_task_id)
 
     # ── Pre-flight: interrupt check ──────────────────────────────────
     if agent._interrupt_requested:
@@ -827,6 +828,7 @@ def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effe
             spinner.stop(f"⚡ {completed}/{num_tools} tools completed in {total_dur:.1f}s total")
 
     # ── Post-execution: display per-tool results ─────────────────────
+    kanban_terminal_succeeded = False
     for i, (tc, name, args, middleware_trace, block_result, blocked_by_guardrail) in enumerate(parsed_calls):
         r = results[i]
         blocked = False
@@ -885,6 +887,14 @@ def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effe
             tool_duration = 0.0
         else:
             function_name, function_args, function_result, tool_duration, is_error, blocked, middleware_trace = r
+            kanban_terminal_succeeded = (
+                kanban_terminal_succeeded
+                or (
+                    function_name in {"kanban_complete", "kanban_block"}
+                    and not is_error
+                    and not blocked
+                )
+            )
             if blocked:
                 effect_disposition = "none"
 
@@ -953,7 +963,7 @@ def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effe
             content=function_result,
             tool_name=name,
             tool_use_id=tc.id,
-            env=get_active_env(effective_task_id),
+            env=execution_env,
             config=_tool_budget,
         ) if not _is_multimodal_tool_result(function_result) else function_result
 
@@ -1014,7 +1024,7 @@ def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effe
     num_tools = len(parsed_calls)
     if finalize and num_tools > 0:
         turn_tool_msgs = messages[-num_tools:]
-        enforce_turn_budget(turn_tool_msgs, env=get_active_env(effective_task_id), config=_tool_budget)
+        enforce_turn_budget(turn_tool_msgs, env=execution_env, config=_tool_budget)
 
     # ── /steer injection ──────────────────────────────────────────────
     # Append any pending user steer text to the last tool result so the
@@ -1023,9 +1033,11 @@ def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effe
     if finalize and num_tools > 0:
         agent._apply_pending_steer_to_tool_results(messages, num_tools)
 
+    return kanban_terminal_succeeded
 
 
-def execute_tool_calls_sequential(agent, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0, *, finalize: bool = True) -> None:
+
+def execute_tool_calls_sequential(agent, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0, *, finalize: bool = True) -> bool:
     """Execute tool calls sequentially (original behavior). Used for single calls or interactive tools.
 
     ``finalize=False`` skips the end-of-batch aggregate budget enforcement
@@ -1034,6 +1046,8 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
     """
     # Resolve the context-scaled tool-output budget once per turn.
     _tool_budget = _budget_for_agent(agent)
+    execution_env = get_active_env(effective_task_id)
+    kanban_terminal_succeeded = False
     for i, tool_call in enumerate(assistant_message.tool_calls, 1):
         # SAFETY: check interrupt BEFORE starting each tool.
         # If the user sent "stop" during a previous tool's execution,
@@ -1571,6 +1585,14 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
         # Log tool errors to the persistent error log so [error] tags
         # in the UI always have a corresponding detailed entry on disk.
         _is_error_result, _ = _detect_tool_failure(function_name, function_result)
+        kanban_terminal_succeeded = (
+            kanban_terminal_succeeded
+            or (
+                function_name in {"kanban_complete", "kanban_block"}
+                and not _is_error_result
+                and not _execution_blocked
+            )
+        )
         # The agent-runtime tools above (todo, session_search, memory,
         # context-engine, memory-manager, clarify, delegate_task) are
         # dispatched inline — they never reach handle_function_call, so the
@@ -1649,7 +1671,7 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
             content=function_result,
             tool_name=function_name,
             tool_use_id=tool_call.id,
-            env=get_active_env(effective_task_id),
+            env=execution_env,
             config=_tool_budget,
         ) if not _is_multimodal_tool_result(function_result) else function_result
 
@@ -1728,7 +1750,7 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
     # ── Per-turn aggregate budget enforcement ─────────────────────────
     num_tools_seq = len(assistant_message.tool_calls)
     if finalize and num_tools_seq > 0:
-        enforce_turn_budget(messages[-num_tools_seq:], env=get_active_env(effective_task_id), config=_tool_budget)
+        enforce_turn_budget(messages[-num_tools_seq:], env=execution_env, config=_tool_budget)
 
     # ── /steer injection ──────────────────────────────────────────────
     # See _execute_tool_calls_parallel for the rationale. Same hook,
@@ -1736,10 +1758,12 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
     if finalize and num_tools_seq > 0:
         agent._apply_pending_steer_to_tool_results(messages, num_tools_seq)
 
+    return kanban_terminal_succeeded
+
 
 
 
-def execute_tool_calls_segmented(agent, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0, segments=None) -> None:
+def execute_tool_calls_segmented(agent, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0, segments=None) -> bool:
     """Execute a mixed tool-call batch as ordered parallel/sequential segments.
 
     ``segments`` is the ``(kind, calls)`` plan from
@@ -1763,24 +1787,28 @@ def execute_tool_calls_segmented(agent, assistant_message, messages: list, effec
     tool_call_id.
     """
     from types import SimpleNamespace
+    execution_env = get_active_env(effective_task_id)
 
     if segments is None:
-        _active_env = get_active_env(effective_task_id)
-        _exec_cwd = Path(_active_env.cwd) if _active_env is not None and _active_env.cwd else None
+        _exec_cwd = Path(execution_env.cwd) if execution_env is not None and execution_env.cwd else None
         segments = _plan_tool_batch_segments(assistant_message.tool_calls, execution_cwd=_exec_cwd)
 
+    kanban_terminal_succeeded = False
     for kind, calls in segments:
         segment_message = SimpleNamespace(tool_calls=list(calls))
         if kind == "parallel":
-            execute_tool_calls_concurrent(
+            segment_succeeded = execute_tool_calls_concurrent(
                 agent, segment_message, messages, effective_task_id, api_call_count,
                 finalize=False,
             )
         else:
-            execute_tool_calls_sequential(
+            segment_succeeded = execute_tool_calls_sequential(
                 agent, segment_message, messages, effective_task_id, api_call_count,
                 finalize=False,
             )
+        kanban_terminal_succeeded = (
+            kanban_terminal_succeeded or segment_succeeded
+        )
 
     # ── Whole-turn finalize (budget + /steer) ─────────────────────────
     total_tools = len(assistant_message.tool_calls)
@@ -1788,11 +1816,13 @@ def execute_tool_calls_segmented(agent, assistant_message, messages: list, effec
         _tool_budget = _budget_for_agent(agent)
         enforce_turn_budget(
             messages[-total_tools:],
-            env=get_active_env(effective_task_id),
+            env=execution_env,
             config=_tool_budget,
         )
         agent._apply_pending_steer_to_tool_results(messages, total_tools)
 
+    return kanban_terminal_succeeded
+
 
 __all__ = [
     "execute_tool_calls_concurrent",
diff --git a/run_agent.py b/run_agent.py
index 6dbfae8..8be90dd 100644
--- a/run_agent.py
+++ b/run_agent.py
@@ -6215,7 +6215,7 @@ class AIAgent:
         self._set_tool_guardrail_halt(decision)
         return toolguard_synthetic_result(decision)
 
-    def _execute_tool_calls(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0) -> None:
+    def _execute_tool_calls(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0) -> bool:
         """Execute tool calls from the assistant message and append results to messages.
 
         The segment planner splits the batch into maximal contiguous runs of
@@ -6335,12 +6335,12 @@ class AIAgent:
         body = ("\n" + indent).join(out_lines)
         return f"{indent}{label}{body}"
 
-    def _execute_tool_calls_concurrent(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0) -> None:
+    def _execute_tool_calls_concurrent(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0) -> bool:
         """Forwarder — see ``agent.tool_executor.execute_tool_calls_concurrent``."""
         from agent.tool_executor import execute_tool_calls_concurrent
         return execute_tool_calls_concurrent(self, assistant_message, messages, effective_task_id, api_call_count)
 
-    def _execute_tool_calls_sequential(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0) -> None:
+    def _execute_tool_calls_sequential(self, assistant_message, messages: list, effective_task_id: str, api_call_count: int = 0) -> bool:
         """Forwarder — see ``agent.tool_executor.execute_tool_calls_sequential``."""
         from agent.tool_executor import execute_tool_calls_sequential
         return execute_tool_calls_sequential(self, assistant_message, messages, effective_task_id, api_call_count)
diff --git a/tests/run_agent/test_run_agent.py b/tests/run_agent/test_run_agent.py
index 539dbdd..6b4256a 100644
--- a/tests/run_agent/test_run_agent.py
+++ b/tests/run_agent/test_run_agent.py
@@ -2391,6 +2391,57 @@ class TestExecuteToolCalls:
         assert messages[0]["role"] == "tool"
         assert "search result" in messages[0]["content"]
 
+    def test_successful_kanban_terminal_result_is_returned(self, agent):
+        tc = _mock_tool_call(
+            name="kanban_complete", arguments='{"summary":"done"}', call_id="c1"
+        )
+        mock_msg = _mock_assistant_msg(content="", tool_calls=[tc])
+
+        with patch(
+            "run_agent.handle_function_call",
+            return_value=json.dumps({"ok": True, "task_id": "t_1"}),
+        ):
+            completed = agent._execute_tool_calls(mock_msg, [], "task-1")
+
+        assert completed is True
+
+    def test_kanban_completion_reuses_pre_transition_environment(self, agent):
+        tc = _mock_tool_call(
+            name="kanban_complete", arguments='{"summary":"done"}', call_id="c1"
+        )
+        mock_msg = _mock_assistant_msg(content="", tool_calls=[tc])
+        messages = []
+
+        with (
+            patch(
+                "run_agent.handle_function_call",
+                return_value=json.dumps({"ok": True, "task_id": "t_1"}),
+            ),
+            patch(
+                "agent.tool_executor.get_active_env",
+                side_effect=[None, AssertionError("stale worker lease")],
+            ) as get_env,
+        ):
+            completed = agent._execute_tool_calls(mock_msg, messages, "task-1")
+
+        assert completed is True
+        assert get_env.call_count == 1
+        assert messages[-1]["name"] == "kanban_complete"
+
+    def test_rejected_kanban_terminal_result_keeps_conversation_running(self, agent):
+        tc = _mock_tool_call(
+            name="kanban_complete", arguments='{"summary":"done"}', call_id="c1"
+        )
+        mock_msg = _mock_assistant_msg(content="", tool_calls=[tc])
+
+        with patch(
+            "run_agent.handle_function_call",
+            return_value=json.dumps({"status": "error", "error": "stale lease"}),
+        ):
+            completed = agent._execute_tool_calls(mock_msg, [], "task-1")
+
+        assert completed is False
+
     def test_sequential_memory_remove_notifies_provider_with_tool_result(self, agent):
         old_text = "stale preference entry"
         tc = _mock_tool_call(
diff --git a/complete-after/tests/tools/test_kanban_role_boundaries.py b/tests/tools/test_kanban_role_boundaries.py
new file mode 100644
index 0000000..edae1a2
--- /dev/null
+++ b/tests/tools/test_kanban_role_boundaries.py
@@ -0,0 +1,81 @@
+"""Regression tests for worker versus live-orchestrator Kanban authority."""
+
+
+def test_kanban_guidance_in_orchestrator_prompt(monkeypatch, tmp_path):
+    monkeypatch.delenv("HERMES_KANBAN_TASK", raising=False)
+    home = tmp_path / ".hermes"
+    home.mkdir()
+    (home / "config.yaml").write_text("toolsets:\n  - kanban\n")
+    monkeypatch.setenv("HERMES_HOME", str(home))
+    from pathlib import Path
+
+    monkeypatch.setattr(Path, "home", lambda: tmp_path)
+
+    from model_tools import _clear_tool_defs_cache
+    from tools.registry import invalidate_check_fn_cache
+
+    invalidate_check_fn_cache()
+    _clear_tool_defs_cache()
+
+    from run_agent import AIAgent
+
+    agent = AIAgent(
+        api_key="test",
+        base_url="https://openrouter.ai/api/v1",
+        model="deepseek-v4-flash",
+        quiet_mode=True,
+        skip_context_files=True,
+        skip_memory=True,
+    )
+    prompt = agent._build_system_prompt()
+    assert "Kanban orchestration protocol" in prompt
+    assert "Kanban task execution protocol" not in prompt
+    assert "Do not execute a task body yourself" in prompt
+    assert "# Tool-use enforcement" not in prompt
+    assert "Keep working until the task is actually complete" not in prompt
+
+
+def test_kanban_guidance_selection_separates_worker_and_orchestrator():
+    from agent.prompt_builder import (
+        KANBAN_GUIDANCE,
+        KANBAN_ORCHESTRATOR_GUIDANCE,
+        select_kanban_guidance,
+    )
+
+    assert select_kanban_guidance(
+        task_id="t_worker", tool_names={"kanban_show", "kanban_list"}
+    ) == KANBAN_GUIDANCE
+    assert select_kanban_guidance(
+        task_id=None, tool_names={"kanban_show", "kanban_list"}
+    ) == KANBAN_ORCHESTRATOR_GUIDANCE
+    assert select_kanban_guidance(
+        task_id=None, tool_names={"kanban_show"}
+    ) == ""
+
+
+def test_kanban_orchestrator_guidance_forbids_worker_substitution():
+    from agent.prompt_builder import KANBAN_ORCHESTRATOR_GUIDANCE
+
+    assert "Do not execute a task body yourself" in KANBAN_ORCHESTRATOR_GUIDANCE
+    assert "substitute `delegate_task`" in KANBAN_ORCHESTRATOR_GUIDANCE
+    assert "A `ready` or `running` task is queued" in KANBAN_ORCHESTRATOR_GUIDANCE
+    assert "Short tasks need not emit heartbeats" in KANBAN_ORCHESTRATOR_GUIDANCE
+    assert "Do not create a replacement" in KANBAN_ORCHESTRATOR_GUIDANCE
+    assert "Worker lifecycle belongs to the assigned worker" in (
+        KANBAN_ORCHESTRATOR_GUIDANCE
+    )
+    assert "Claim success only when" in KANBAN_ORCHESTRATOR_GUIDANCE
+
+
+def test_kanban_worker_lifecycle_gate_excludes_orchestrator(monkeypatch):
+    from tools import kanban_tools as kanban
+
+    monkeypatch.delenv("HERMES_KANBAN_TASK", raising=False)
+    monkeypatch.setattr(kanban, "_profile_has_kanban_toolset", lambda: True)
+    assert kanban._check_kanban_mode()
+    assert kanban._check_kanban_orchestrator_mode()
+    assert not kanban._check_kanban_worker_mode()
+
+    monkeypatch.setenv("HERMES_KANBAN_TASK", "t_worker")
+    assert kanban._check_kanban_worker_mode()
+    assert not kanban._check_kanban_orchestrator_mode()
diff --git a/tests/tools/test_kanban_tools.py b/tests/tools/test_kanban_tools.py
index 1cabc82..2b3ca13 100644
--- a/tests/tools/test_kanban_tools.py
+++ b/tests/tools/test_kanban_tools.py
@@ -1,8 +1,7 @@
 """Tests for the Kanban tool surface (tools/kanban_tools.py).
 
 Verifies:
-  - Tools are gated on HERMES_KANBAN_TASK: a normal chat session sees
-    zero kanban tools in its schema; a worker session sees the kanban set.
+  - Kanban tools are partitioned between normal, worker, and orchestrator sessions.
   - Each handler's happy path.
   - Error paths (missing required args, bad metadata type, etc).
 """
@@ -119,7 +118,7 @@ def test_worker_with_kanban_toolset_still_hides_board_routing(monkeypatch, tmp_p
 
 
 def test_kanban_tools_visible_with_toolset_config(monkeypatch, tmp_path):
-    """Orchestrator profiles with toolsets: [kanban] see all kanban tools."""
+    """Orchestrators get board-routing tools, not worker lifecycle tools."""
     monkeypatch.delenv("HERMES_KANBAN_TASK", raising=False)
     home = tmp_path / ".hermes"
     home.mkdir()
@@ -135,10 +134,8 @@ def test_kanban_tools_visible_with_toolset_config(monkeypatch, tmp_path):
     names = {s["function"].get("name") for s in schema if "function" in s}
     kanban = {n for n in names if n and n.startswith("kanban_")}
     expected = {
-        "kanban_list",
-        "kanban_show", "kanban_complete", "kanban_block", "kanban_heartbeat",
-        "kanban_comment", "kanban_create", "kanban_link",
-        "kanban_unblock",
+        "kanban_list", "kanban_show", "kanban_comment", "kanban_create",
+        "kanban_link", "kanban_unblock",
         "kanban_attach", "kanban_attach_url", "kanban_attachments",
     }
     assert kanban == expected, f"expected {expected}, got {kanban}"
@@ -1530,10 +1527,19 @@ def test_kanban_guidance_in_worker_prompt(monkeypatch, tmp_path):
     assert "kanban_complete" in prompt
     assert "kanban_block" in prompt
     assert "kanban_create" in prompt
+    assert "rejected `kanban_complete` call does not end the task" in prompt
     # Anti-shell guidance
     assert "Do not shell out" in prompt or "tools — they work" in prompt
 
 
+def test_unblock_schema_forbids_speculative_retry():
+    from tools import kanban_tools as kt
+
+    description = kt.KANBAN_UNBLOCK_SCHEMA["description"]
+    assert "blocker is confirmed resolved" in description
+    assert "Never unblock merely to poll, retry speculatively" in description
+
+
 def test_kanban_guidance_prompt_size_bounded(monkeypatch, tmp_path):
     """Sanity: the guidance block stays lean so it doesn't blow up the
     cached prompt.
@@ -1553,7 +1559,7 @@ def test_kanban_guidance_prompt_size_bounded(monkeypatch, tmp_path):
     monkeypatch.setattr(_P, "home", lambda: tmp_path)
 
     from agent.prompt_builder import KANBAN_GUIDANCE
-    assert 1_500 < len(KANBAN_GUIDANCE) < 5_500, (
+    assert 1_500 < len(KANBAN_GUIDANCE) < 5_800, (
         f"KANBAN_GUIDANCE is {len(KANBAN_GUIDANCE)} chars — too short (missing?) or too long"
     )
 
@@ -1562,17 +1568,14 @@ def test_kanban_guidance_prompt_size_bounded(monkeypatch, tmp_path):
 # Worker task-ownership enforcement (regression tests for #19534)
 # ---------------------------------------------------------------------------
 #
-# A worker process has HERMES_KANBAN_TASK set to its own task id. The
-# destructive tools (kanban_complete, kanban_block, kanban_heartbeat,
-# kanban_unblock) must refuse to operate
-# on any OTHER task id, even if the caller supplies an explicit `task_id`
-# argument. Workers legitimately call kanban_show / kanban_list /
-# kanban_comment / kanban_create / kanban_link on other tasks, so those
-# are unrestricted.
+# A worker process has HERMES_KANBAN_TASK set to its own task id. Lifecycle
+# handlers must refuse to mutate any other task even if an explicit task_id is
+# supplied. Workers may inspect, comment on, create, or link related tasks.
 #
-# Orchestrator profiles (no HERMES_KANBAN_TASK in env) are intentionally
-# exempt — their job is routing, and they sometimes close out child
-# tasks on behalf of the child.
+# Calls with no worker identity remain available to trusted internal/operator
+# recovery code. Model-facing live orchestrators do not receive complete,
+# block, or heartbeat definitions because the registry applies the separate
+# worker lifecycle gate.
 
 
 def test_worker_complete_rejects_foreign_task_id(worker_env):
@@ -1764,9 +1767,8 @@ def test_worker_complete_rejects_stale_run_id(worker_env, monkeypatch):
     assert d.get("ok") is True
 
 
-def test_orchestrator_complete_any_task_allowed(monkeypatch, tmp_path):
-    """Orchestrator profiles (no HERMES_KANBAN_TASK) can still complete
-    any task via explicit task_id. The check only applies to workers."""
+def test_unscoped_complete_handler_retains_operator_recovery(monkeypatch, tmp_path):
+    """The internal handler remains usable outside a model-facing worker."""
     monkeypatch.delenv("HERMES_KANBAN_TASK", raising=False)
     home = tmp_path / ".hermes"
     home.mkdir()
diff --git a/tools/kanban_tools.py b/tools/kanban_tools.py
index 6dbf760..111b0e5 100644
--- a/tools/kanban_tools.py
+++ b/tools/kanban_tools.py
@@ -96,6 +96,11 @@ def _check_kanban_attachment_mode() -> bool:
     return _check_kanban_mode()
 
 
+def _check_kanban_worker_mode() -> bool:
+    """Worker lifecycle tools require a dispatcher-assigned task identity."""
+    return bool(os.environ.get("HERMES_KANBAN_TASK"))
+
+
 def _check_kanban_orchestrator_mode() -> bool:
     """Board-routing tools (kanban_list, kanban_unblock) are intentionally
     hidden from task workers.
@@ -1853,9 +1858,9 @@ KANBAN_CREATE_SCHEMA = {
             "max_runtime_seconds": {
                 "type": "integer",
                 "description": (
-                    "Per-task runtime cap. When exceeded, the "
-                    "dispatcher SIGTERMs the worker and re-queues the "
-                    "task with outcome='timed_out'."
+                    "Per-task runtime cap. When exceeded, the dispatcher "
+                    "terminates the worker, records outcome='timed_out', and "
+                    "applies the configured retry and circuit-breaker policy."
                 ),
             },
             "initial_status": {
@@ -1968,17 +1973,18 @@ def _kanban_create_schema_overrides() -> dict:
 KANBAN_UNBLOCK_SCHEMA = {
     "name": "kanban_unblock",
     "description": (
-        "Unblock a Kanban task. It moves to ready when all parents are done, "
-        "or todo while any parent remains open. Orchestrator-only — only "
-        "profiles with the kanban toolset can unblock routed work; "
-        "dispatcher-spawned task workers never see this tool."
+        "Unblock a Kanban task only after the blocker is confirmed resolved, or "
+        "the task contract or a human explicitly authorizes a retry. Never "
+        "unblock merely to poll, retry speculatively, or override a rejected "
+        "completion. Orchestrator-only; dispatched task workers never see this "
+        "tool."
     ),
     "parameters": {
         "type": "object",
         "properties": {
             "task_id": {
                 "type": "string",
-                "description": "Blocked task id to move to ready or parent-gated todo.",
+                "description": "Blocked task id whose blocker is confirmed resolved.",
             },
             "board": _board_schema_prop(),
         },
@@ -2032,7 +2038,7 @@ registry.register(
     toolset="kanban",
     schema=KANBAN_COMPLETE_SCHEMA,
     handler=_handle_complete,
-    check_fn=_check_kanban_mode,
+    check_fn=_check_kanban_worker_mode,
     emoji="✔",
 )
 
@@ -2041,7 +2047,7 @@ registry.register(
     toolset="kanban",
     schema=KANBAN_BLOCK_SCHEMA,
     handler=_handle_block,
-    check_fn=_check_kanban_mode,
+    check_fn=_check_kanban_worker_mode,
     emoji="⏸",
 )
 
@@ -2050,7 +2056,7 @@ registry.register(
     toolset="kanban",
     schema=KANBAN_HEARTBEAT_SCHEMA,
     handler=_handle_heartbeat,
-    check_fn=_check_kanban_mode,
+    check_fn=_check_kanban_worker_mode,
     emoji="💓",
 )
 
